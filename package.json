{
  "name": "website",
  "version": "0.1.0",
  "homepage": "https://adityakeerthi.github.io",
  "private": true,
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d _site",
    "start": "eleventy --serve",
    "build": "eleventy",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "gh-pages": "^4.0.0"
  }
}
